<!-- afficher contenu de la page 
copie de index.php -->
	<div id="content" class="hfeed">

		<?php get_template_part( 'loop-meta' ); // Loads the loop-meta.php template. ?>

		<?php if ( have_posts() ) { ?>

			<?php while ( have_posts() ) { ?>

				<?php the_post(); // Loads the post data. ?>
				
				<?php get_template_part( 'content', ( post_type_supports( get_post_type(), 'post-formats' ) ? get_post_format() : get_post_type() ) ); ?>

			<article><h2>Tous les témoignages</h2>	
			pour tous les témoignages pour lesquels display = 1
			<ul>
				<li>afficher image "mise en avant" - celle qui est avec le nom</li>
				<li>Afficher l'extrait du témoignage</li>
				<li>Afficher le nom du témoin</li>
				<li>Afficher la photo du témoin (s'il y a)</li>
				<li>Afficher le prix</li>				
				<li>Afficher le lien vers le témoignage entier</li>
			</ul>
			

			<h3>afficher l'id de tous les témoignages du groupe 0</h3>
			<h5>l'array qui est renvoyé par la fonction</h5>
			<?php 
			$group = 0 ;	// pas de sélection de groupe		
			$liste = ac_list_testimonial( $group ) ;
			var_dump( $liste ) ;
			?>
			<p></p>
			</br>
			<h3>impression de tous les éléments recueillis</h3>

			<?php
			foreach ( $liste as $item ) {
				/* récupère toutes les données du témoignage */
				$full_testimonial = ac_testimonial_everything( $item[ 'testimonial_id' ] ) ;  
				if( !empty( $full_testimonial ) ) { 
					?>
					<h5>id : 
						<?php echo $item[ 'testimonial_id' ] ; ?>
						, Groupe : 
						<?php echo $item[ 'group_id' ] ; ?>
					</h5>

					<p>ID : 
					<?php echo $full_testimonial[ 'id' ] ;?>
					</p>
					<p>nom : 
					<?php echo $full_testimonial[ 'nom' ] ;?>
					</p>				
					<p>extrait : 
					<?php echo $full_testimonial[ 'extrait' ] ;?>
					</p>
					<p>Budget : 
					<?php echo $full_testimonial[ 'budget' ] ;?>
					</p>				
					<p> Photo Uploaded : 
					<?php echo $full_testimonial[ 'photo' ] ;?>
					<a href="<?php echo $full_testimonial[ 'photo' ] ;?>"><img class="alignnone size-medium wp-image-599" src="<?php echo $full_testimonial[ 'photo' ] ;?>" alt="uni_plus-332x400" width="249" height="300"></a>
					</p>
					<p>Image mise en avant : 
					<p><?php echo $full_testimonial[ 'photo-1' ] ;?></p>
					<p><?php var_dump( $full_testimonial[ 'photo-1' ] )  ;?></p>
					<p>afficher toutes les photos</p>
					<?php 
					foreach ( $full_testimonial[ 'photo-1' ] as $item ) { 
						echo $item ;
					} ?>
					<p>afficher uniquement la première photo</p>
						<?php echo $full_testimonial[ 'photo-1' ][ 0 ] ; ?>
					<p>texte complet : 
					<?php echo $full_testimonial[ 'temoignage' ] ;?>
					</p>
					<p>Date : 
					<?php echo $full_testimonial[ 'date' ] ;?>
					</p>
					<p>Read More link : 
					<?php $read = ac_read_full_link() ; ?>
					<a href="<?php echo $read[ 'link' ] ; ?>/?testimonial_id=<?php echo $full_testimonial[ 'id' ] ; ?>"><?php echo $read[ 'text' ] ; ?></a>
					</p>
				<?php 
				} 
			} ?>

				</br>
			<h3>contenu d'un array par id</h3>
			<p> test avec $Id = 7</p>
			<?php
			$Id = 7 ;

			$full_testimonial = ac_testimonial_everything( $Id ) ;
			var_dump( $full_testimonial ) ;
			?>
			
			


			<?php } ?>



			
			</article>
		<?php } else { ?>

			<?php get_template_part( 'loop-error' ); // Loads the loop-error.php template. ?>

		<?php } ?>

		<?php get_template_part( 'loop-nav' ); // Loads the loop-nav.php template. ?>

	</div><!-- #content -->
	
<?php get_footer(); // Loads the footer.php template. ?>	